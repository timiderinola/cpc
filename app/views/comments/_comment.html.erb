<p><%= comment.author %> said:</p>

<blockquote>
  <%= raw comment.content %>
</blockquote>

<%= link_to 'Remove comment', [comment.message, comment],
            method: :delete, data: { comfirm: 'Are you sure?' } %>